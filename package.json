{
  "name": "todo-mvc-service",
  "version": "0.0.1",
  "description": "mock API for TodoMVC apps",
  "repository": "https://github.com/alexkrolick/todo-mvc-service",
  "main": "dist/todo-mvc-service.min.js",
  "module": "lib/index.js",
  "scripts": {
    "test": "echo \"We'll get there someday\" && exit 1",
    "build:dist": "NODE_ENV=production webpack --config=webpack.config.js",
    "build:lib": "NODE_ENV=development babel src --out-dir lib",
    "watch": "babel src --out-dir lib --watch",
    "prepublish": "npm run build:dist && npm run build:lib"
  },
  "keywords": ["todomvc", "mock"],
  "author": "Alex Krolick",
  "license": "MIT",
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",
    "find-my-way": "^1.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-stage-1": "^6.24.1",
    "prettier": "^1.6.1",
    "webpack": "^3.5.6"
  }
}
